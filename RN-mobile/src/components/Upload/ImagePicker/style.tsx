import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    paddingTop: 10,
    marginBottom: 30,
  },
  buttonWrapper: {
    flexDirection: "row",
    marginRight: 10,
    gap: 10,
  },
  imageWrapper: {
    flexDirection: "row",
    gap: 10,
  },
});
