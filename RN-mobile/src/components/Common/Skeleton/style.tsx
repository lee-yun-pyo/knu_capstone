import { StyleSheet } from "react-native";
import { Skeleton } from "constant/color";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: Skeleton.BG_GRAY,
    borderRadius: 6,
  },
});
