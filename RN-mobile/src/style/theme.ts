import { DefaultTheme, Theme } from '@react-navigation/native';

export const theme: Theme = {
    ...DefaultTheme,
    dark: false,
    colors: {
        primary: "#03C75A",
        background: "#FFFFFF",
        card: "#FFFFFF",
        border: "#E2E2E2",
        text: "#000000",
        notification: "#000000"
    }
}